export const metadata = { title: "About — Static Export" };

export default function AboutPage() {
  return (
    <main>
      <h1>About</h1>
      <p>A static App Router page with no dynamic data.</p>
    </main>
  );
}
