import { ActionButton } from "../action-button";
import { $$hoist_0_adminOnly } from "../actions/admin";
export default function Page() {
  return <ActionButton id="admin" action={$$hoist_0_adminOnly} />;
}
